# Scripts

This directory is reserved for helper scripts that support:
- inventory rendering
- validation
- preflight checks
- rebuild orchestration
- backup verification

Only scripts that are safe to publish and maintain should be copied here.
